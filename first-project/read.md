1.1 สร้างตัวcxiโดยใช้ let มารับค่าแล้วใช้console.log ให้แสดงผลทางหน้าจอ

1.2 สร้างตัวมามารับค่าทั้ง 4 ชนิด แล้วใช้ typeof เช็คชนิดของมัน

1.3 สร้างตัวแปรมารับค่าทั้ง 3 ตัว สร้างตัวแปรมารับค่า sum และ avg ที่ทำการคิดแล้ว แสดงผลแแกทางหน้าจอ

1.4 สร้างตัวแปรมาเก็บค่า username password age แล้วใช้ if else มาสร้างเงื่อนไข